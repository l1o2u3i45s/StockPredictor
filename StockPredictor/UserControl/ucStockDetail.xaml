<UserControl x:Class="StockPredictor.UserControl.ucStockDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockPredictor.UserControl"
             xmlns:viewModel="clr-namespace:StockPredictor.ViewModel" 
             d:DataContext="{d:DesignInstance Type=viewModel:StockDetailViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource AccordionArrowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <TextBlock Text="{Binding StockID}"></TextBlock>
        </Grid>
    </Grid>
</UserControl>
